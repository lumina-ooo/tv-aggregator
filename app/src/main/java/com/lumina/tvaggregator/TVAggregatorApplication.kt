package com.lumina.tvaggregator

import android.app.Application

class TVAggregatorApplication : Application() {

    override fun onCreate() {
        super.onCreate()
        // Initialize any global components here if needed
        // For example: analytics, crash reporting, etc.
    }
}